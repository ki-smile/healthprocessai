[tool:pytest]
testpaths = tests
python_paths = .
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings

# Add the current directory to Python path
pythonpath = .

[tool:coverage:run]
source = core
omit = 
    tests/*
    */test_*
    */__pycache__/*